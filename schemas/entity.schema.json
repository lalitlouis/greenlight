{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greenlight.dev/schemas/entity.schema.json",
  "title": "Entity",
  "description": "A clearance-relevant thing named in the script. One Parallel research task per entity.",
  "type": "object",
  "required": ["entity_id", "type", "surface", "scene_ids", "context"],
  "additionalProperties": false,
  "properties": {
    "entity_id": {"type": "string", "pattern": "^E[0-9]{3,4}$"},
    "type": {
      "type": "string",
      "enum": ["BRAND", "TRADEMARK", "MUSIC", "PERSON", "LOCATION", "ARTWORK", "VEHICLE", "FILM_CLIP", "PUBLICATION", "ORGANIZATION"]
    },
    "surface":    {"type": "string", "description": "Exact text as written in the script."},
    "scene_ids":  {"type": "array", "minItems": 1, "items": {"type": "string"}},
    "context":    {"type": "string", "description": "The sentence(s) around it. What the researcher actually needs."},
    "depicted_negatively": {
      "type": "boolean",
      "description": "Load-bearing. Flips a brand from 'send a courtesy letter' to 'will never clear'."
    },
    "prominence": {"type": "string", "enum": ["BACKGROUND", "FEATURED", "PLOT_CRITICAL"]}
  }
}
