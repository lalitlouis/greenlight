# Frozen contracts

These JSON Schemas are the interface between Person A (agents) and Person B (data/UI).
Freeze them on day 1. Change them only by talking to each other first.

Flow: Script -> Scene[] -> Entity[] -> Flag[] -> Report
